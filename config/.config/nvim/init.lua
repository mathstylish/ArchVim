-- basic configuration (no plugins have been configured here)
require "user.settings"
require "user.keymaps"
require "user.autocmds"

-- plugin imports (if you need to use bare minimum, e.g working on tty console, comment all below this line)
require "user.plugins" -- plugin installer
require "user.core" -- plugins configuration dir